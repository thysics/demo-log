{
  "name": "task-management-frontend",
  "version": "1.0.0",
  "description": "Frontend for Task Management App",
  "main": "src/index.js",
  "scripts": {
    "start": "esbuild src/index.js --bundle --outfile=public/bundle.js --servedir=public --define:process.env.NODE_ENV='\"development\"' --define:process.env.REACT_APP_API_URL='\"http://localhost:5000\"' --loader:.js=jsx",
    "build": "node build.js"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.2",
    "esbuild-plugin-copy": "^1.3.0"
  }
}